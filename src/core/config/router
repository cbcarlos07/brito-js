const controller = require('../../controllers/{{import}}.controller')
const BaseRouter = require('../base.router')


class {{className}}Router extends BaseRouter{
    prefix = '/{{route}}'
    constructor(controller){
        super(controller)
    }
}

module.exports = new {{className}}Router(controller)